/**
 * DisplayUsedCards Vue Component unit tests
 * By Austin Ball    on: 24/05/19
 */

import DisplayUsedCards from '../../../src/components/SharedComponents/DisplayUsedCards.vue'

describe('DisplayUsedCards.vue', () => {
  /* it('has created hook', () => {
    expect(typeof DisplayUsedCards.created).to.equal('function')
  }) */

  /* it('sets correct default data', () => {
    expect(typeof DisplayUsedCards.data).to.equal(this.getCurrentPlayer())
    const defaultData = DisplayUsedCards.data()
    expect(defaultData.player).to.equal(this.getCurrentPlayer())
  }) */

  it('computes object', () => {
    expect(typeof DisplayUsedCards.computed).to.equal('object')
  })

  /* it('test groupableStack', () => {
    expect(Stack.computed.groupableStack.call()).to.equal(false)
  }) */
})
